#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
#include <unordered_map>
#include <fstream>

using namespace std;

void convert_vector(){
    //run me once to convert a vector easily
    fstream fs;
    fs.open("inputfile.txt");
    while(!fs.eof())
}